name: Update Statuses

on:
  schedule:
    - cron: "0 * * * *"      # запускаем каждый час
  push:
    branches: [ main ]       # и при каждом твоём коммите в main
  workflow_dispatch:          # плюс ручной запуск из вкладки Actions

permissions:
  contents: write             # нужно, чтобы бот мог пушить изменения

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          persist-credentials: true

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "18"

      - name: Run update script
        run: node scripts/update.js

      # 🔧 Исправленный шаг коммита: добавляем все изменения в data/ и коммитим только если есть что коммитить
      - name: Commit & push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          # Добавляем все изменения в папке data (и новые файлы тоже)
          git add -A data

          # Если изменений нет — выходим без ошибки
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "chore: auto-sync khoindvn [skip ci]"
            git push
          fi
